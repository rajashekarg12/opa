# REST connector and global credential references captured during build.
# Used with credentialsPlan.properties at deploy time.
#
# Integration (OIC) connectors are not captured. They resolve through the target
# workspace Registered Service, which is created manually once per environment.

application.name=ExpenseApprovalApp
application.version=1.0

